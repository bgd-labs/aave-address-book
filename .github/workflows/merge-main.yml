name: Head branch workflow

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    uses: bgd-labs/github-workflows/.github/workflows/foundry-test.yml@f74fa202fb42ada4a6b008dc64f82487c2fefb81
  release:
    uses: bgd-labs/github-workflows/.github/workflows/release.yml@main
